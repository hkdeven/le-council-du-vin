# Le Council du Vin

> _in vino veritas_

[![Netlify Status](https://api.netlify.com/api/v1/badges/b91ca720-46e1-481e-ba82-17fc682cdd9e/deploy-status)](https://app.netlify.com/projects/le-council-du-vin/deploys)
&nbsp;
![Supabase](https://img.shields.io/badge/Supabase-project%20live-3ECF8E?logo=supabase&logoColor=white)
&nbsp;
![Access](https://img.shields.io/badge/access-login%20live-3ECF8E?logo=supabase&logoColor=white)
&nbsp;
![Next.js](https://img.shields.io/badge/Next.js-14-000?logo=nextdotjs&logoColor=white)
![TypeScript](https://img.shields.io/badge/TypeScript-strict-3178C6?logo=typescript&logoColor=white)
![Tailwind](https://img.shields.io/badge/Tailwind-3-38BDF8?logo=tailwindcss&logoColor=white)

<!--
  STATUS BADGES — keep these current.
  • Netlify: live deploy-status badge for site le-council-du-vin (site id
    b91ca720-46e1-481e-ba82-17fc682cdd9e).
  • Supabase: project is provisioned and the schema is applied. There is no
    per-project status badge; platform health lives at https://status.supabase.com
  • Access: the login wall (Google SSO + branded magic link) is LIVE in
    production — NEXT_PUBLIC_ENFORCE_LOGIN=true in Netlify, Supabase env present,
    custom SMTP via Resend. Local/preview stays open (demo) by default.
-->

A private web app for a monthly blind wine-tasting society. Each moon, 10–12 members
convene under a rotating theme, bring a bottle, cloak them in numbered black cloths,
score each pour 1–10, then lift the cloths and tally the reckoning.

The aesthetic is **The Deep** — cold true-black, tarnished gold, hairline strokes, moon
phases and alchemical glyphs. Old-world secret-society, playing off the all-seeing-eye
emblem.

## Screens

| Screen | Purpose |
| --- | --- |
| **Gate** | Landing / sign-in. Two buttons — members enter, outsiders petition. Login options appear only after clicking "Enter the council": Google, password, or magic link, plus **Forgot password** (recovery mail → the public `/reset` page, where a new secret word is forged + confirmed; requires the site's `/reset` URL in Supabase Auth → Redirect URLs). New souls create accounts via Google or magic link. |
| **Initiation** | Cult-style application — chosen name, email, **date + time of birth** (the chart is drawn from these; no more star-sign/element questions), ritual questions, the oath. Public; the petition persists (`src/lib/applications.ts` in demo, `applications` table live) and surfaces in the Keiser's tribunal. Links back to the gate. |
| **Convene** | The next meeting front-and-centre plus **future gatherings** (collapsed by default) and a Keiser **"summon a new gathering"**. Each meeting (Keiser can **edit** theme, supporting line, host, date, and time via a pencil): rotating host, editable **observances** + **Keiser's decree** copy (seeded defaults), host **venue** (prefilled from the host, editable here without syncing back to their profile), **RSVP** (the last section of the meeting card; members self-RSVP, the Keiser can RSVP on anyone's behalf), and the Keiser bottle count. Each member can privately log **"your offering"** behind a full-width button that unfurls a bordered box — the wine they plan to bring plus an **optional price and grape** (auto-detected from the name), hidden from everyone (even the Keiser); once sealed it shows as a locked box with edit/erase. At the card's tail, **"Consult the Prophecy"**: once every RSVP'd soul has sealed an offering, the vine names who it expects to be crowned — weighing bringer history, the room's grape leanings, and its price weaknesses — speaks once, stores the verdict, and is graded at the Reckoning. **The button stays dark until that gate is met** (counted from the moment the card is drawn, recounted as souls answer, and kept shut if the count cannot be taken), rather than inviting a tap and refusing it. Sleeping souls are excluded, since they cannot seal an offering and counting them would silence the vine forever. A verdict already spoken always reveals: it is a matter of record and cannot be un-known, which is why a prophecy spoken too early is cleared in the vault rather than hidden (the gate used to sit inside `if (!g.prophecy)`, so it was consulted only before the FIRST word and every later tap skipped it entirely; found 2026-08-10 on gathering 18, spoken a month earlier when three of seven had answered). A full-bleed gold rule sits under the theme. **Enter the rite** sits inside the meeting card (above the host line) and **opens 30 minutes after the scheduled start** — the rite and reveal tabs are hidden from the nav entirely until then (the pages themselves stay locked as backstop), and the button appears on its own when the window opens (no refresh). Moon-phase divider before "Gatherings to come". The whole card is **shareable to WhatsApp** (bold-formatted details + `RSVP here` link, for non-members; the link-preview image is the black-gold logo via OpenGraph). The Keiser can also **cancel** a gathering. |
| **The rite** | Blind scoring — a focused card (a designed gold **cloth medallion** for the wine number `1–20`, from `public/cloths/` (WebP, all preloaded on entry so switching wines is instant; text fallback beyond 20)), 1–10 orb verdict showing its number, random 3–4 aroma prompts per wine from a 100-aroma bank plus **add-your-own**, and notes — plus a **ballot** of every wine you can re-score in one tap, so a later pour can unseat earlier favourites. Sealing **persists the ballot** (`src/lib/ballots.ts`; revising after breaks the seal) and surfaces a **"Proceed to the revelation"** button. |
| **The rite** | Score each wine on ten orbs, one cloth at a time, revising freely until you seal. **One field of words per wine, titled "Aromas"** (the aroma pills and their typed input were retired 2026-08-21: members could not tell where the Nose cloud came from, and the typed box was shared across wines so an unadded word followed you to the next bottle). Those words are exactly what the Nose mines, and aromas marked on older ballots still count. **One button, "Seal and continue"**, which seals and carries you to the revelation; there is no second click. **A sealed reckoning is final**: the scoring card is not drawn at all, only a read-only list of the verdicts cast. That is not cosmetic, touching a single orb rewrote the ballot as `sealed: false`, which dropped the reveal's count and re-locked the night for the whole table (found on the first real night, 2026-08-21). |
| **Reveal** | **Hidden (nav + page) until the rite opens, then locked until every RSVP'd attendee has a sealed ballot** (computed from real ballots, no seed). While locked, the panel **names the souls still to judge** under a "Still to judge" heading (chips, your own marked "(you)"), not merely the sealed count, so the table knows whose phone to look for; the roll read that decides who is waited on carries `cult_name` for it (demo mode reads the local roster), a soul whose name has not loaded yet is still listed unnamed so the count and the list can never disagree, and sleeping or cast-out attendees are absent from both. Scores are **real tallies** — the average of every sealed ballot per cloth. The champion card sits **face-down** (the "Turn the card, crown the goddess" art, `public/reveal-back.webp`) — click to flip, then the ranked bottles rise one by one. **Ties share the crown** — several top-score bottles are shown together as co-champions and all recorded as rank 1. Each member **claims their own bottle** (one per soul; claiming pulls in their pre-registered offering; **release** undoes a mistake); titles are click-to-edit. The **Keiser can disqualify** off-theme wines — they lose their rank, fall to the bottom struck-through, and feed the **disqualification ledger** (threshold {5} → expulsion hearing). When all is recorded, **"Commit it to the Annals"** (visible to all, Keiser-only action) locks the result into the codex — and the page **transforms into The Reckoning**: gold-banded sections for the Crowning, the ranked table, the split cloth (the night's most divisive bottle), the table's whispers (anonymous quotes from the notes), the value ledger (best points-per-rand when prices are known), the prophecy's grade, the photo gallery, and an **Email this to me** self-send. After commit only the Keiser may amend, via the codex. |
| **Oracle** | Forecasting the moons ahead. **Date polls** (multiple can run at once; any member creates a poll, adds dates, and votes; each date expands an **accordion of who voted**; polls can be **archived/restored**; shareable to WhatsApp). Then **The Unexplored** (grapes the Council has never poured, tappable into the proposal box, plus the finest neglected grape) joined by a gold rule to the theme idea-pool (propose with an optional supporting line + favour; the Keiser can **edit or delete** themes), and the hosting wheel — sections separated by moon-phase dividers. |
| **Tribunal** | Open to every full member. Petitioners (name + birth-derived sun sign + their answers) + the Council's non-binding tally + the final decree. **Anoint as initiate** creates their account at initiate rank (limited access — Convene, Rite, Reveal only) and drops them into the profile roster, where the Keiser can later **Elevate** them to full member. Pending petitions and **Past petitioners** live in their own sections; every petitioner opens as an animated card (portrait or the pyramid mark, gate answers, live tally, **no email shown**). Members cast their counsel (anoint / cast out / abstain; one vote each via the cast_counsel RPC, changeable until the decree), and after the decree **the record names every vote forever**. A shimmering **Consult the Augury** button flips the card to a computed reading: accord with the table (kuta vs every complete member chart), who they sit easiest and hardest beside, the palate omen, and the shadow — veiled unless the petition carries a full birth record, which the gate now REQUIRES (date, time, and place of birth). The decree and the anointment date stay the Keiser's alone. Also **expulsion hearings**: members with five disqualifications are summoned — all members vote keep/cast-out, the Keiser decrees. |
| **Codex** | a.k.a. **the Annals** — same page, not a separate one. Historical stats (metrics row: gatherings, bottles judged, chalice champion, **coin poured** in rand, **prophecies fulfilled** as a % — NA until the first prophesied night is sealed), a **Grapes tasted** progress bar (distinct varietals recorded vs the 177-grape ledger), the **committed reckonings** (each carries **"Email this to me"**: that night's full Reckoning email, reachable forever, not only until the next gathering is summoned), and the **disqualification ledger** with the threshold-of-five flag. **The Reliquary** (all-time records: highest pour, the great schism, iron palate, gentle hand, **the crowned grape**, **the price of victory** with the table's average in its tooltip — all on the shared Tip component) sits under the metrics; the **disqualification ledger** closes the page. **The curse of the first cloth** (average verdict by pouring order, counted only where the pour was recorded, positions need enough nights to chart) sits above **Past gatherings**, which lists every committed night newest-first; each expands to theme / date / host(s) / the ranked wines (title, owner, **varietals and price** when known, score, DQs marked) plus the night's **split cloth** when a wine divided the table by 5+. **Naming an owner brings their wine with it**: pick a member in the codex editor and the offering they sealed before the night (title, price, grapes) fills whatever is still blank, never overwriting the Keiser's own hand. The **Keiser can amend any past night in place** (pencil on an expanded night: theme, number, date, host + co-host, every wine's **cloth (pour number — editable, blank = pouring order never recorded; blank-cloth wines never feed pour-order stats; on unscored nights the LISTED order carries the crown, never the cloth)** / title / owner / score / off-theme flag / **grapes (multi-select, auto-detected from the name) / price**, add or remove wines — ranks re-reckoned from scores on seal, ties share the crown), **record a past gathering manually** (owners are free text with a member datalist, so departed souls can be credited), and **erase** a night entirely. Every night carries a **"Look upon the wine" photo gallery** any member may add to (sleeping members included, by decree; **the Keiser alone may take one down**, which removes it from the night AND from the bucket, after a confirm), and **unowned bottles show "claim it"** so a member can take credit (one bottle per soul per night). Claiming goes through **`claim_bottle`**, a SECURITY DEFINER door that sets the owner on ONE unowned row for the calling member: the annals are Keiser-only to write, as the record must be, so before this every member but the Keiser was simply refused ("new row violates row-level security policy for table annals", hit by the whole table on the first real night). It refuses a stranger, a sleeping member, a bottle already spoken for, and a second bottle on a night where the caller already holds one. Ballot data never reaches the codex until the Keiser's "Commit it to the Annals" on the reveal, or his own hand here. **Sixteen nights of pre-app history (Feb 2025 onward) were imported** via `scripts/import-annals.ts` — real per-voter ballots, notes, and departed-soul profiles included. |
| **Profile** | Editable identity — click the avatar circle to upload + crop a portrait, click the name to rename, set an optional **title** (60 chars; worn on the card in place of the moon phase), edit venue instructions, and set date, time, **timezone, and Place of Birth** (geocoded once; coordinates stored invisibly). From birth, a set of **read-only, tooltipped** fields is derived (`src/lib/astrology.ts`): western element + sun sign, moon sign, the **exact** ascendant (sidereal time at the birth place), and Chinese Shengxiao (animal) + Wu Xing (five-element, with a per-result tooltip). **The council roster** lives here for every rank — members and initiates see the roll read-only (portraits open cards), while the **Keiser** gets every account in an accordion, each editable in place (cult name, email, initials, rank, venue instructions, active flag); edits persist (`src/lib/members.ts`) and flow through to where members are listed (Convene RSVP + host picker, Oracle host wheel + poll voters). A **Feature request** box relays any member's wish to the Keiser's inbox (recipient forced server-side). Reached via the header chip; in open mode it also hosts the "view as" tier switcher, in enforced mode the sign-out. |

## Roles

- **Initiate** — anointed but not yet trusted. Limited access: **Convene, the Rite, and the Reveal only** (no Oracle, Codex, or Tribunal). The intended onboarding path — a prospective member fills the initiation rite, the Keiser anoints them to initiate, then elevates them to full member once they've stood a gathering.
- **Member** — sees everything.
- **Keiser** — super-admin. Final say on membership regardless of the vote, elevates initiates → members from the profile roster, plus hosting and historical edits.

### Keiser-only powers

Everything below is visible/actionable **only to the Keiser** — other members either don't see it at all, or see it read-only. (Enforced in the UI via `isKeiser`, and in the database via the `is_keiser()` RLS helper.)

- **Tribunal (entire screen)** — hidden from the nav for everyone else. Review petitions and **anoint** (→ initiate) or **cast out**; run **expulsion hearings** for members who reach five disqualifications and decree mercy or expulsion. A **petition-count badge** on the Tribunal tab flags new initiates on login.
- **Profile → the council roster** — view **every** member's account and edit it in place (cult name, email, initials, **rank**, venue instructions, active flag); **elevate** an initiate to full member; **cast a member out** of the Council (which also removes their petition record).
- **Profile → Heralds** — compose and send branded emails: the new-gathering **invite** (defaults to all active members) and the tribunal **summons** (to a member at the disqualification threshold), editing recipients before sending.
- **Convene** — **summon** a new gathering and **cancel** one; **edit** the meeting (theme, supporting line, host, date, time, the observances, the Keiser's decree, and venue) via the top pencil; set/adjust the **wine count**; and **RSVP on anyone's behalf**.
- **The rite** — add or remove wines mid-tasting (the **wine-count** stepper). Everyone else sees the count but can't change it.
- **Reveal** — **disqualify / restore** off-theme wines; **"Commit it to the Annals"** (all members see the button, but only the Keiser can commit); once committed, **only the Keiser may amend** the locked result.
- **Oracle** — **edit** or **cast out** themes from the pool. (Proposing themes, casting favour, and creating/voting on date polls are open to all members.)

For contrast, any **member** can: RSVP, log a private offering, score in the rite, claim and name their bottle on the reveal, propose and favour themes, create and vote on polls, and read the codex.

## Notable features

- **The gate ledger** (Keiser-only, on the profile) — every sign-in, who/when/from what vessel, written by `/api/log-login`. **It needs `SUPABASE_SERVICE_ROLE_KEY` in the Netlify environment** and no-ops silently without it (which is exactly what it did, unnoticed, from the day it shipped until 2026-07-17: `POST /api/log-login` answers `{"skipped":"not configured"}` when the key is absent, which is the quickest way to check). The key is required because `login_events` deliberately has no insert policy, so this route is the only door in. Throttled to once per 30 minutes per browser, so a member browsing all evening is logged once.
- **The door is case-blind** — a member's access hangs on their auth address matching their `members` row. Both the app's lookup and the RLS policy compared **exactly**, so one capital letter locked a member out of the Council entirely, showing them "known to the gate, but not yet of the Council" forever (found 2026-08-10: James had been anointed on 8 July and shut out ever since by `Jamesmilne.badenhorst@` versus the lowercase address Supabase hands the app). Now canonical at both ends: the app trims and lowercases before looking anyone up, every door that writes an address stores it canonically (the gate, anointing, the roster editor on blur), and **all twelve** email comparisons in `supabase/policies.sql` use `lower()`, including `is_keiser()`, `is_member()`, `is_awake()`, `cast_counsel()` and `add_reveal_photo()`. That last part mattered: a member with an odd address could otherwise read their own row yet still be refused the roster, their counsel, and their standing. The repair SQL (canonicalising what is already stored) sits at the end of the policies file.
- **The calendar summons** — answering a call on Convene posts that night to the member's own calendar. The letter ("Your place is set") carries **`invite.ics`** and plain instructions for iPhone/Mac, Gmail, and everything else; the file itself embeds an alert the evening before, fired by their device, not by us. Built to the parts that actually break invites (`src/lib/ics.ts`, **38 checks** in `scripts/verify-ics.ts`): `METHOD:REQUEST` in the body **and** on the attachment's `content_type` **and** an `ATTENDEE` line naming the recipient (the three things that decide event-versus-download); CRLF endings; folding at 75 **octets** that never splits a character or an escape pair; the four RFC 5545 escapes; UTC `Z` times (no `VTIMEZONE`, the classic "an hour out" bug), proven against a daylight-saving zone; and a **stable UID per gathering** with a rising `SEQUENCE`, so a re-send amends the night instead of duplicating it. Sent **only to the caller's own address** (the route enforces it), so RSVPing for someone else mails nobody. **Withdrawing sends nothing** by decree: the night stays in their calendar until they remove it. Prove the whole path before members see it via **Profile → Heralds → Prove the calendar summons**, or open `public/email-previews/invite-sample.ics` directly to test the file alone. Known limit: Resend sends the file as an attachment rather than a `multipart/alternative` calendar part, so Apple Mail shows a proper event card while **Gmail may show a preview without a one-tap add**.
- **Transactional emails** — branded, mobile-first HTML (`src/lib/emailTemplates.ts`, previewable under `public/email-previews/`) sent via **Resend** through a Keiser-gated route (`/api/send-email`). Automatic: petition **anointed** (→ initiate) and initiate **elevated** (→ member). Manual from **Profile → Heralds**, with add/remove recipients: new-gathering **invite** and tribunal **summons**. Plus a branded **magic-link** template for Supabase Auth. Titles use a cursive display face, a moon-phase divider from the gate, and an Instagram footer. Everything no-ops safely until `RESEND_API_KEY` / SMTP are set.
- **Member cards** — every member avatar across the app (Convene, Oracle, Tribunal, Profile roster) is clickable to a tarot-style stats card: portrait (click it again to enlarge in a small lightbox), name, their **title** if they have chosen one (otherwise **moon phase at birth**), role, then the Palate Dossier, the chalice count, and only then the birth-derived fields (tooltipped, never the raw date/time of birth; they sit just above The Heavens button) — element, sun, moon, ascendant, Shengxiao, Wu Xing, **Bazi day-master, life path number, Venus sign, and birth arcana** (each with a label tooltip *and* a per-result meaning tooltip) — **Value for coin** and **The purse** (their bottle economics against the table's, once 2+ of their bottles carry prices), and a **chalice count**, one gold chalice per codex victory. Animated open/close (`src/components/MemberCard.tsx`).
- **Exact natal engine** (`src/lib/astrology.ts`) — no lookup tables where astronomy exists: sun sign from the true solar ecliptic longitude (no cusp-day drift), moon sign from a perturbed lunar ephemeris, Venus from its orbital elements, and the ascendant from the exact sidereal time at the birth coordinates. Birth times convert local → UT through the IANA timezone database (default `Africa/Johannesburg`, selectable). Chinese zodiac honours the actual Chinese New Year boundary (1940–2030 table). Verified against documented reference charts and recorded sky events.
- **Place of Birth** — one text field on the profile and petition; "Mark it" geocodes it (Open-Meteo, keyless) and quietly stores coordinates + timezone for the chart. Missing data never guesses: the ascendant shows "unknown hour/place" instead.
- **Tarot per night** — deterministic draw from the Major Arcana keyed on `(gathering, member)`, so a member's card is fixed for a gathering but re-dealt each event (`src/lib/tarot.ts`).
- **Date voting** — mark every date you can make; the night the most souls can attend leads. A **Share poll** button opens WhatsApp pre-filled with the dates and a link back to the poll (`/oracle`).
- **Keiser-set wine count** — the Keiser fixes how many bottles convene (on Convene) and can add/remove wines mid-rite if the count was off; everyone else sees the count but can't change it. Shared between Convene and the Rite (localStorage in demo; `gatherings.wine_count` when live).
- **Relative re-scoring** — the rite keeps every wine's score, aromas, and notes live at once; a ballot lets you revise any earlier wine while judging a later one (blind tasting is comparative).
- **Aromas** — a 100-aroma bank (`src/lib/aromas.ts`); each wine shows a deterministic random 3–4 as quick prompts (one line on mobile), and the taster can coin new aromas on the spot.
- **Per-member venue instructions** — stored on each member, ready to drop into invites when they host.
- **Branding** — the all-seeing-eye favicon, header logo mark (`public/site-mark.png`), full wordmark logo, and an ornate celestial **card frame** (`public/card-frame.webp`) around the focused wine on the rite; backgrounds keyed to transparency so the art sits on the true-black UI. Header is a single line (logo + "Le Council du Vin") with a compact profile chip — no per-role buttons.

## Roles of access (login)

The login wall is **live in production**, controlled by `NEXT_PUBLIC_ENFORCE_LOGIN`
(independent of whether Supabase is connected).

- **Enforced** (production — `NEXT_PUBLIC_ENFORCE_LOGIN=true`, Supabase env present): every
  screen except the gate and the public petition redirects to login. Signing in isn't enough —
  the email must exist in the `members` table, so a signed-in stranger sees a "pending" screen.
  Login is **Google SSO** or a **branded email magic link** (delivered via Resend SMTP).
- **Open mode** (local/preview default — `NEXT_PUBLIC_ENFORCE_LOGIN` unset/`false`): every
  screen is navigable and a header role switcher walks all three tiers, so the app stays
  browsable without auth. Supabase can still be connected for data.

## Tech stack

- **Framework:** Next.js 14 (App Router) + React 18 + TypeScript
- **Styling:** Tailwind CSS plus a custom stylesheet (`src/app/globals.css`), theme "The Deep"
- **Backend:** Supabase (PostgreSQL + Auth). Client reads its own member row via RLS; secrets stay server-side.
- **Hosting:** Netlify (`@netlify/plugin-nextjs`, see `netlify.toml`)

## Local development

```bash
npm install
npm run dev
```

Without a `.env.local`, the app runs in **demo mode**. The only seeded content is the
real member roster and theme pool (`src/lib/seed.ts`) — gatherings, RSVPs, offerings,
ballots, reveals, and committed reckonings all start empty and persist per-browser in
localStorage (`src/lib/gatherings.ts`, `ballots.ts`, `bottles.ts`, `annals.ts`), so the
whole loop — summon → RSVP → rite → reveal → commit — is exercisable end to end.
Add Supabase env (below) to switch to live mode.

> Don't run `npm run build` while `next dev` is running — they share `.next` and the
> build clobbers the dev server's CSS. Stop the dev server first, or use a clean checkout.

## Going live

1. Create a Supabase project.
2. Run [`supabase/schema.sql`](supabase/schema.sql) in the SQL editor (edit the seeded Keiser email first).
3. Run [`supabase/policies.sql`](supabase/policies.sql) — RLS so a member can read their own row and petitions can be submitted.
4. **Authentication → URL configuration**: set the Site URL and add redirect URLs (`http://localhost:3005` and the Netlify URL).
5. **Authentication → Providers → Google**: enable it with an OAuth client (magic link + password work out of the box).
6. Copy `.env.example` → `.env.local` and fill in `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
7. When ready to require login, set `NEXT_PUBLIC_ENFORCE_LOGIN=true` (locally and in Netlify) and restart. Until then the app stays open/navigable while still reading Supabase.

## Deploy

Connect the repo to Netlify; set the same `NEXT_PUBLIC_SUPABASE_*` env vars in Netlify's
build settings (this is what turns the production login wall on). `@netlify/plugin-nextjs`
handles the Next build.

## Project status

**Done**
- All eight screens in "The Deep", responsive, with seeded demo content.
- Branding: favicon (`src/app/icon.png`), full logo, transparent art on true black.
- **Login wall live in production** (Google SSO + branded magic link via Resend SMTP) + role gating. Initiate onboarding wired end to end: petition → Keiser anoints to initiate (limited access) → elevate to member from the profile roster.
- **Live data on Supabase** — the whole loop (summon → RSVP → rite → reveal → commit) reads/writes live: ballots/scoring, offerings, annals/codex, Oracle themes + polls. Reveal auto-unlocks by polling for sealed ballots; reveal photos persist to Supabase Storage.
- **Profile layout** — Your sky sits above the roster; the roster and venue instructions fold (open by default), Heralds and Feature request fold (closed). The Reckoning's "Email this to me" button shows in every mode (demo explains it has no post office).
- **Sleep (deactivation)** — the Keiser can put any seat to sleep from the roster (profile → the council roster → expand a row → **Put the seat to sleep** / **Wake the seat**; it writes the existing `members.active` flag, no new column). A sleeping member **keeps every reading right their rank grants and may edit nothing, anywhere**: no profile edits, ballots, offerings, RSVPs, counsel, claims, or photos. It is **visible to them** (an app-wide "Your seat sleeps" banner, and every write control disabled rather than left to fail silently) and **to everyone else** (greyed portrait and a `sleeping` tag on the roll and on their member card, and **tarnished chalices**: their victories show greyed while they sleep, never reduced, and brighten the moment they wake). **Every field is disabled, not just the save buttons**: AppShell sweeps `input, textarea, select` under the main content and disables them (a MutationObserver re-sweeps on repaint and on React handing a field back enabled), so a sleeping hand cannot even type; buttons are deliberately NOT swept, since the writing ones are sealed individually and sweeping them would take away reading (tooltips, folds, the Heavens, the lightbox). Opt out with `data-sleep-ok`. **The one exception, by decree: photographs of past nights.** A sleeping member may still hang one, which needs both a bucket carve-out (`reveal-photos`, like `charts`; `avatars` stays sealed since a portrait is their own record) and the **`add_reveal_photo`** SECURITY DEFINER RPC, because the photo list lives in `gatherings.reveal_photos` and RLS cannot open one column without opening the whole row. The one word left to them is a **plea to wake** (`type: "wake"`: the only email the route allows FOR a sleeping caller, carried to the Keiser alone; it writes nothing). Two locks enforce it: (1) the instrument, `src/lib/writeLock.ts` armed by AuthProvider, with **the Supabase client itself wrapped in a Proxy** (`src/lib/supabase.ts`) so every insert/update/upsert/delete, writing RPC, and records-bucket upload throws before it leaves the browser — an audit found most live writes go straight from a component to supabase and never touch the data libs, so sealing the libs alone sealed almost nothing; the lock closes **pessimistically** until the register is truly read, so a stale member cache cannot leave a sleeping hand unlocked. (2) **the vault**, `supabase/policies.sql` → "THE SLEEPING SEAL": an `is_awake()` SECURITY DEFINER helper plus **RESTRICTIVE** policies on every records table and storage (Postgres ORs permissive policies, so only a restrictive one can take a right away). Deliberately untouched: reads, the read-only `reveal_summary` RPC, the `charts` bucket and self-addressed Heavens emails (your own sky is a reading right), and Supabase auth (sleep never locks a soul out of their own door). The reveal never waits on a sleeping or cast-out attendee (that deadlock would cost the night), the vine never waits on their offering, and the Keiser may always **withdraw** a sleeping RSVP though never summon one. `scripts/verify-sleeping.ts` (47 checks).

- **The claim on the night reaches the record (24 Sep 2026)** — the August fix opened `claim_bottle` for claims made on the **codex**, but the reveal page, where every soul actually claims after the cloths lift, wrote a member's claim to that member's React state and nowhere else (`persist` only wrote the annals `if (committed && isKeiser)`). Nobody else's browser ever saw it, the Keiser's commit wrote the Keiser's own local rows, and after the commit the button was gone for members. Every hand but the Keiser's came up unclaimed, two gatherings running. A claim is now **a number on the claimant's own offering row** (`offerings.cloth`, set only from the reveal, never from the offering form): `claimCloth` in `src/lib/bottles.ts` upserts it, the reveal polls the offerings every 5 s until the commit so the whole table sees each name land, `src/lib/revealClaims.ts` (pure) lays the claims over the tallied rows (owner, and the logged wine, grapes and price wherever the Keiser has not written by hand; the Keiser's hand is never overwritten), and the Keiser's commit re-reads the offerings once more before the one write that counts. **One hand per bottle is the vault's rule**: a partial unique index `offerings_one_hand_per_cloth`, so a second hand on a claimed bottle gets 23505 and a plain message rather than a silent loss. After the commit, the annals are the record: the Keiser amends them directly and late claims still go through `claim_bottle` on the codex. `scripts/verify-reveal-claims.ts` (20 checks; the old behaviour fails 9, overwriting the Keiser's hand fails 1) and `supabase/verify-cloth-claim.sql` (12 checks as a genuine non-Keiser member: the claim lands, editing the wine afterwards keeps it, a soul with no logged wine can still claim, a second hand is refused and the first survives, release, two claims stand together, a sleeping hand is refused, every claim is readable by the table; without the index it fails 2; rolls itself back, safe on production). `schema.sql` also now records `offerings.price` and `offerings.varietals`, which were added to the live table by hand and had left the replica without them. **An independent adversarial review of the diff then found six more gaps, all closed the same day**: `fetchAllOfferings` swallowed a failed read and returned `{}`, so one dropped request at the commit tap would have written every bottle as unclaimed (it now throws, the commit refuses, and the poll keeps the last good claims); a member's page open at the commit never learned of it, so a late tap looked accepted and was lost (the poll now reads the annal too and draws the Reckoning); the Keiser's own Reckoning right after the commit showed the record without names until a refresh; **any signed-in member could write any member's offering row** (`policies.sql`: a `my_member_id()` helper now binds insert/update/delete to the owner, Keiser exempt; 3 new checks in `verify-cloth-claim.sql`, 15 in all, and the old loose policy fails 1); erasing a wine on Convene deleted the row and the claim with it (it now blanks the wine and keeps the claim); and a slow poll could land after a tap and paint the old claims over the new one for five seconds (offerings reads are sequence-numbered, only the latest lands). `scripts/verify-offering-form.ts` (6 checks) proves the demo-mode form never asks for or drops a claim. **Driven end to end in the real app** in demo mode with two tabs: the member claims, the Keiser's tab shows the name within one poll, the Keiser claims and disqualifies, commits, the Keiser's Reckoning carries every name at once, the member's untouched tab turns into the Reckoning on its own within five seconds, and the annals and codex carry both owners, the price and the grapes.
- **Bug #6 is closed on production, proven (11 Sep 2026)** — `supabase/verify-claim.sql` was run against the live database and passed 13/13 as a genuine non-Keiser member: a member still cannot write the annals directly (42501, the exact refusal from the night), can claim through `claim_bottle`, is refused a second bottle, an already-claimed bottle, a sleeping hand and a stranger, two members' claims stand together, and `claim_bottle` carries the row lock. The run rolls itself back and leaves nothing behind.
- **RLS and the SECURITY DEFINER doors are testable at last (11 Sep 2026)** — the thing that let bug #6 reach a real gathering was that it could not be tested: the Keiser's own account passes every role test whether or not it works, and the anon key returns an empty list for `members`, `annals`, `gatherings` and `ballots` (RLS, by design), which looks exactly like "no data". `./scripts/local-replica.sh` now builds a throwaway local Postgres from the **real** `schema.sql` and `policies.sql` plus a faithful stand-in for the Supabase furniture (`authenticated`/`anon` and `auth.jwt()`/`auth.uid()` defined as Supabase defines them, reading `request.jwt.claims`), so **any member can be impersonated safely**. `supabase/verify-claim.sql` (13 checks) then proves the claim as a genuine non-Keiser member: it builds a throwaway member and night, becomes them, and asserts that a member still **cannot** write the annals directly (42501, the exact refusal from the night) but **can** claim through `claim_bottle`, that a second bottle, an already-claimed bottle, a sleeping hand and a stranger are all refused, and that a second member's claim does not erase the first. It **cannot commit**: the whole run is one DO block that always ends by raising, which prints the report and rolls everything back, so it is safe to run against production. `./scripts/verify-claim-race.sh` proves the row lock with **two genuinely concurrent connections**: against a deliberately unlocked copy of the function one claim is silently lost every run while both members are told they succeeded, and against the real one both survive. That script rewrites `claim_bottle` while it runs, so it refuses any remote connection string and restores the real function from `policies.sql` on exit.
- **The two decisions that decide everything are now pure and guarded (11 Sep 2026)** — the rite's screen choice (`src/lib/riteScreen.ts`) and the auth door's row match (`src/lib/memberLookup.ts`) were both inline in components, where the Council's two worst bugs lived and where nothing could be tested. Both are extracted and held by suites: `scripts/verify-rite-guard.ts` (21 checks, **exhaustive over all 32 state combinations**, enforcing the rule that the writable scoring card is reachable ONLY when every state it depends on is genuinely known) and `scripts/verify-member-lookup.ts` (33 checks, including James's exact stored address, leading/trailing space, and the `_`/`%` wildcard cases where `ilike` can match a DIFFERENT member's row and the code comparison must refuse it). Both suites were **mutation-tested**: nine deliberately broken versions were fed in, including the exact pre-fix code of each bug, and every one was caught (the original rite ladder fails 12 checks; the original exact-match lookup fails 7). Twelve suites, **342 checks**.
- **Pre-gathering hardening (11 Sep 2026)** — a testing pass before the coming night found the August seal-fix incomplete and five other silent failures. **The rite's guards were all written `if (ready && ...)`**, so before the fetches resolved every guard was false and the page fell through to a live, writable scoring card: one tap in that window wrote `sealed:false` over a sealed ballot (reproduced: six verdicts reduced to one). The rite now shows a waiting mark until BOTH the gathering and the member's own ballot have landed, and only then the gate, the sealed view, or the card; a verdict cast before the gathering loaded used to be written against gathering `"none"` and dropped in silence, so `gid` stays null until real and a refused draft save now shows under the ballot. **`claim_bottle` took no row lock** (`for update`), so simultaneous claims, which is how every claim happens, overwrote each other with both members told they had succeeded. **`annals` had no permissive DELETE policy**, so erasing a night matched zero rows for everyone including the Keiser and PostgREST called it success. **`saveOffering`, `favourTheme` and `deleteApplicationsByEmail` ignored their `error`**, making their callers' existing `.catch` alerts dead code (a refused offering showed as sealed while the Prophecy went on waiting for it). **`AuthProvider` ignored the members-fetch error** and set `member` to null, so one dropped request showed a full member the "your petition awaits" screen and cached that null; a failed read is now held apart from a genuine non-member. The Prophecy's dark button explained itself only through a `title` (invisible on a phone) and now carries a shared `Tip` naming the live unsealed count. Also **the case-blind door was only half done**: every comparison in `policies.sql` used `lower()`, but `AuthProvider`'s own lookup was still an exact `.eq("email", ...)`, so access still hung on every stored address happening to be canonical. RLS would hand a member their row while the query failed to ask for it, which is the exact shape of the fault that shut James out. The lookup is now `.ilike` plus a canonical comparison in code (ilike alone treats `_` and `%` as wildcards, so the code comparison is what decides the match). **SQL for the live DB is in `TEST-LOG.md` under 2026-09-11; both statements were run against production on 11 Sep 2026.**
- **The new-gathering date field is labelled** — a `type="date"` input ignores `placeholder` entirely, so on a phone it was simply a blank cell with no clue what it wanted. It now carries a visible **"The night"** label, with **"The host"** beside it.
- **`public/flow-prototype.html`** — a self-contained, clickable walkthrough of the whole voting flow (convene → rite → reveal) with a panel to puppet the rest of the table, so the gates can be exercised without seven phones: the prophecy dark until every offering is sealed, the single seal button, the locked sealed ballot, the reveal held until every ballot is in, and claiming one bottle per soul.
- **The curtain nav** — the tab row is retired; the chambers fold behind a hamburger in the topbar (icon turns to an X, curtain drops beneath the bar, full-width rows, gold-lit current chamber). The Tribunal keeps its petition-count badge inside the curtain and the closed hamburger wears a small wine dot while petitions await. Navigation closes the curtain. The role text beside the topbar avatar is retired; the profile ring stands alone.
- **Fast on mobile** — the signed-in member row and every heavy list (codex annals, gatherings, members) use a stale-while-revalidate cache (`src/lib/swr.ts` + `lcv_member_cache`): pages paint instantly from the last local snapshot and refresh silently. Member **portraits live in the public `avatars` Storage bucket** (short cacheable URLs, not base64 in the table; `scripts/migrate-avatars.ts` moves legacy ones). Loading spinners (`src/components/Loading.tsx`) cover only genuine first visits.
- **Transactional email system** (Resend) — auto anoint/elevate, manual invite + tribunal summons from Profile → Heralds, branded magic link, Keiser petition alert.
- **Clickable member cards** (tarot stats + chalice count), **tie/co-champion** handling on the reveal, rite **opens 30 min after the scheduled start**.
- Tarot per night, date-voting poll + WhatsApp share, custom aromas, orb-number verdict, per-member venue instructions.
- GitHub repo + Supabase project provisioned, schema applied.
- **The astral suite** (built to the member-approved mockup, `public/member-card-mockup.html`): **The Palate Dossier** on every member card — moons stood, bottles crowned, marks against, palate temper, kindred palate, the hosting wheel, longest communion, the aroma **Nose** cloud (drawn from marked aroma pills, typed custom aromas, and whispered notes mined in full; filler words and wine-noise are filtered out everywhere, including the rite's custom-aroma input and imported history's comments), and their finest pours, all derived from real ballots + annals. **The natal chart** — the full ten-planet wheel (verified against documented positions), whole-sign houses, aspect lines, tap-a-row placement meanings. **The Foretelling** — three views in one segmented pill: **This day** (the Moon's house crossing + fast-planet aspects exact today), **This moon** (new/full moons in their houses, slow-planet transits with true dates, Mercury retrogrades), and **The year** (personal year, Chinese year, house ingresses). All computed, never invented; every passage hand-written and **spoken in plain English on the page** (the emails keep the Council tongue). Both gated: missing time/place of birth shows "the sky is veiled" linking to the profile, never a guess. **Co-hosts** — two hosts per gathering, both credited on the wheel. **Your sky** on the profile — open either view or email it to yourself (the wheel travels as a snapshot image).

- **The wine-data suite** (built to approved mockups): optional **price + varietals** on every offering and in the codex editor (auto-detected from the wine's name, multi-select picker, `src/lib/varietals.ts` — a **177-entry ledger** of grapes, Cape locals, and blend/style categories with alias detection; the Oracle's **Unexplored** draws from a curated notable list and a standing **dessert-wine ban** (`NEVER_SUGGEST`) keeps Hanepoot and kin off every suggestion surface, now and in future features). **The split cloth** on codex nights, **The Reliquary** records card, **The Prophecy** (all-data pre-reveal prediction: owner form, per-taster grape leans and price affinity; spoken once, stored, graded on the Reckoning), the **Reckoning** view after commit with email-to-me, and coin rows (Value for coin, The purse) on member cards. The Oracle refetches when you return to the tab and shows a loading veil on first consult.
- **Convene/codex hardening** (mobile session): Convene never shows a past night as current; summon list waits for the live roster; codex **"Brought by" is a dropdown** (roster, departed souls, Another name…); past nights record with **no names and/or no scores** (unscored rows show "—", never drag averages); **off theme · DQ** works in history and feeds the Disqualifications ledger; erasing a night deletes the gathering first and surfaces refusals instead of half-failing.
- **The Vedic engine** (`src/lib/vedic.ts`, no libraries) — Lahiri ayanamsa (verified against published values), mean-node Rahu/Ketu, whole-sign houses from the sidereal Lagna, 27 nakshatras with padas, the Navamsa (D9), and Vimshottari maha+antar dashas with true dates; **24/24 checks** in `scripts/verify-vedic.ts`. The Kundli UI is designed and approved (`public/kundli-mockup.html`: North/South chart toggle, the current dasha age, kindred-stars kuta matching between full members, muhurta day chips, detected yogas; `public/natal-mockup.html`: temperament bars, aspect figures, chart bearer, birth moon for the Western chart). **The Kundli is now built**: a third door on the profile (with its envelope, a full branded kundli email) and a third button on every member card open the modal — North/South chart toggle (remembered per device), Lagna/nakshatra/navamsa rows with hand-written tooltips for all 27 birth stars and 12 risings, **kindred stars** (classical ashta-koota accord out of 36 between full members, both charts complete), the current **dasha age** with its antardasha tempering and true dates, the full Vimshottari turning, the pillars and the pits, **Muhurta · Favourable Hours** (tara bala + day lords, real dates), the marriage bond (with an honest mangal dosha note when present), and the **yogas** (Gajakesari, Budhaditya, Chandra-Mangala, Kemadruma, the five Mahapurusha — only those truly present are named). Every passage speaks in first person on your own chart and third person on another's. `scripts/verify-kundli.ts`: 33 checks over the classical tables, 729-pair invariants, and an independently confirmed chart.
- **The audit**: an independent research pass quadruple-checked every constant, table, and formula in the Vedic and kuta engines against JPL Horizons, the Meeus reference implementation, live DrikPanchang values, and the classical tables. Everything confirmed except three table corrections (tara kuta even-remainder rule, gana and vashya orientation), all applied; `verify-kundli.ts` runs **45 checks**, `verify-vedic.ts` 24. The methodology sections now carry the doctrine note: no astrology API of any kind is consulted; everything is computed in-house.
- **Crowns survive disqualifications** — `championsOf()` in `src/lib/annals.ts` is the single source of truth for who took a night: rank 1 non-DQ as normal; a DQ'd first place is never credited and the best surviving score is promoted; an unscored night (imported history) still crowns the sole wine left standing after disqualifications, and leftover blank rows can never block that crown (the editor no longer seals empty rows at all). Wired through the codex crownings + victory ledger + chalice champion, member-card chalices, the dossier, and the prophecy's grade.
- **THE HEAVENS (built)**: one door for all of the sky. Every member card's sky buttons became a single **The Heavens**; tapping it **turns the card over** in 3D (return arrow + a footer control turn it back; one close for everything; the turned-away face never intercepts taps, the card's height breathes to the active face). Behind the door, three stops — **The Wheel · The Foretelling · The Kundli**. The Wheel now carries the temperament (element/mode bars + hand verdict), the figures (aspect patterns detected, never invented), the chart bearer, and the birth moon. The Foretelling's day/moon/year views each read under a **Western/Vedic lens pill** (remembered per device): the Vedic side brings the day's star (tara bala + Chandra bala), the five-limb almanac (panchang, with the yoga summed from sidereal longitudes as the classics demand), the gochara ledger (vedha omitted and the methodology says so), the year's exact turnings, the pratyantar clock, and **the iron passage** — Sade Sati with its three phases and Ashtama Shani, dated by true sign entries that honour Saturn's retrograde wobbles. The profile's Your sky is the same single door; **every stop ends with "Email me this view"** (four templates, incl. the Vedic foretelling). Engines in `src/lib/gochara.ts` + `src/lib/natal-analysis.ts`; five verification suites now guard **127 checks**.
- **THE ATLAS (built 24 Sep 2026, to `public/atlas-mockup.html`)**: the Wheel laid upon the world (astrocartography). The Wheel stop now carries its own lens pill, **The sky · The earth** (remembered per device, `lcv_wheel_lens`): the sky is the wheel as before; the earth is the Atlas. `src/lib/atlas.ts` takes the SAME planet positions as the wheel (no new ephemeris, no outside service), derives each planet's RA/declination, and draws four lines per planet: overhead and underfoot are the meridians `RA - GMST` and its antipode; rising and setting are the horizon curves `φ = atan(-cos H / tan δ)` with H the hour angle (H < 0 rising). Ecliptic latitude is ignored (the Moon's lines may drift ~1°). The map is an inline equirectangular SVG (`src/lib/atlas-svg.ts`; coasts from world-atlas land-110m baked into `src/lib/atlas-land.ts`), five planets shown by default, tap a glyph to isolate one and mark its nearest cities. Below it, **Where the sky favours you**: cities within 600 km grouped by plain questions (Love = Venus setting/rising, Luck and money = Jupiter overhead/rising, Work and reputation = Sun overhead, Home = Moon underfoot, Hard lessons = Saturn rising/overhead), each row saying how far the line passes and Strong (< 200 km) / Noticeable (< 400) / Faint (< 600). **Ask of a place**: the same place search as the profile, then an "In short" verdict and one plain sentence per line within reach (40 hand-written planet-by-line sentences in `LINE_SAY`). **The council's map**, **full members and the Keiser only** (initiates never see it on any card): every member's lines for one theme (Luck / Love / Work / Home) with initials at the top, the viewer's brighter, and the cities where two or more members have a line within reach. Reads the roster through the existing "members roster read" policy; no schema change. City table `src/lib/atlas-cities.ts`: GeoNames cities15000 at 250k+ **with suburbs kept on purpose**, plus a few small places worth naming. Every row names the exact nearest place (Benoni, 10 km) and, in a quieter voice, the largest city of a million or more within 120 km ("near Johannesburg", `metroOf`/`nearLabel`); places sharing a metro collapse into one row, so a list is never four suburbs of Johannesburg. The Keiser's decree: the closest place first, the major city second with less emphasis. Copy rule for this feature: **plain English, not the oracular voice.** Its envelope (`emailAtlas`, type `atlas`, self-send) carries the map as a snapshot and the five city lists. `scripts/verify-atlas.ts` (41 checks: the Sun's underfoot line ~21° west of a 02:00 EST birthplace, overhead opposite underfoot, rising and setting never share a longitude, every sampled point lies on its own line, distances against a known great circle, ask-of-a-place ordering and reach, the council gathering).
- Automated **reminder emails** are intentionally **not** sent (declined by design: no nagging before a gathering). The one automatic letter is the **calendar summons** on answering a call (below), which is a receipt rather than a nudge, and the day-before alert it carries is fired by the member's own calendar, not by us. New-gathering invites and tribunal summons remain **manual**, Keiser-triggered from Heralds. Automated WhatsApp would still need the WhatsApp Business API; manual share links cover it.
- Member voting UI on applications (the Council's tally is display-only for now; the Keiser's decree is binding).
- Richer Codex charts.
- **Blind-tasting privacy** — ballots/offerings are still API-readable before the reveal; owner-only RLS until the gathering is revealed is a known gap (accepted by the Keiser for the trusted club; the Prophecy deliberately reads sealed offerings under the same trust).
- **The Tome** (approved, backlogged) — each year's codex as an in-app page-turn book: a cover, the year's records, one leaf per night (crowning, whispers, photos), one leaf per soul (their "Wrapped": bottles judged, average given, most-used word, hardest dissent). Opened by a gold button under the codex metrics; shareable by link; printable file possible later. Not a PDF by design.
- **Standing honours** (approved, backlogged) — auto-earned badges worn on the member card: seasonal (Iron Palate, The Gentle Hand, Unbroken Communion, The Lamplighter, First Blood, Back-to-Back, The Kingslayer, The Schismatic, The Treasure Hunter…) and lifetime (The Centurion, The Founding Soul, The Grape Sage…). ~26 candidates compiled awaiting the Keiser's cull; seasonal honours feed the Tome.
- **Post-reveal digest email to all members** — the Reckoning currently mails on request (self-send); an automatic send to the whole table on commit is a natural next step.
- Prophecy refinements — the spoken-once rule may loosen later, per the Keiser.

_The `MEMORY`/notes for this project live in the assistant's memory, not the repo._
